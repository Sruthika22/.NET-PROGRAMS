using System;
class Program
{
    static void Main()
    {
        string str = "madam";
        string rev = new string(str.Reverse().ToArray());
        Console.WriteLine(str == rev ? "Palindrome" : "Not Palindrome");
    }
}
