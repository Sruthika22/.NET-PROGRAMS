using System;
class Program
{
    static void Main()
    {
        int num = 153, sum = 0, temp = num;
        while (temp > 0)
        {
            int rem = temp % 10;
            sum += rem * rem * rem;
            temp /= 10;
        }
        Console.WriteLine(sum == num ? "Armstrong" : "Not Armstrong");
    }
}
